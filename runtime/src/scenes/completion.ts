import Phaser from "phaser";

// M4: celebration animation + parent rating capture
export class CompletionScene extends Phaser.Scene {
  constructor() {
    super({ key: "CompletionScene" });
  }

  create() {
    // TODO M4
  }
}
