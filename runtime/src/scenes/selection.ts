import Phaser from "phaser";

// M4: loads activity library and presents categorised grid for parent to pick from
export class SelectionScene extends Phaser.Scene {
  constructor() {
    super({ key: "SelectionScene" });
  }

  create() {
    // TODO M4
  }
}
