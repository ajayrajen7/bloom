import Phaser from "phaser";

// M1/M4: loads ActivityJSON, renders mechanic, handles interactions
export class ActivityScene extends Phaser.Scene {
  constructor() {
    super({ key: "ActivityScene" });
  }

  create() {
    // TODO M1
  }
}
